<project name="ccp-common-build-script-setup" default="import-build-scripts" basedir="..">

	<property name="build.branch.name" value="branches/RB-1.0" />

	<property name="ant.dir" value="ant" />
	<property name="ant.external.lib.dir" value="${ant.dir}/lib" />
	<property name="build.scripts.dir" value="${ant.dir}/.build-scripts" />
	<property file="${ant.dir}/svn.properties-DO_NOT_REDISTRIBUTE" />

	<path id="svn.classpath">
		<fileset dir="${ant.external.lib.dir}">
			<include name="**/svn*.jar" />
		</fileset>
	</path>
	<typedef resource="org/tigris/subversion/svnant/svnantlib.xml" classpathref="svn.classpath" />

	<!-- checks out a branch of the ccp/build project into the build scripts directory (ant/.build-scripts) -->
	<target name="import-build-scripts">
		<delete dir="${build.scripts.dir}" />
		<mkdir dir="${build.scripts.dir}" />
		<echo message="Checking out build scripts into ${build.scripts.dir}" />
		<svn username="${svn.build.user}" password="${svn.build.passwd}" svnkit="true">
			<checkout url="http://compbio.ucdenver.edu/svn/ccp/build/${build.branch.name}" revision="HEAD" destPath="${build.scripts.dir}" />
		</svn>
	</target>
</project>