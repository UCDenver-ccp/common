<project name="ccp-common" default="build" basedir="..">
	
	<!-- List internal project dependencies here (comma-delimited) -->
	<property name="dependency.list" value="" />
	<path id="classpath.compile.dependency" />
	
	<import file="build-base.xml" />
</project>
